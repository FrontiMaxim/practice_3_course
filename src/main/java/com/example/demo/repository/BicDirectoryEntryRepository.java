package com.example.demo.repository;

import com.example.demo.models.BicDirectoryEntry;
import org.springframework.data.repository.CrudRepository;

public interface BicDirectoryEntryRepository extends CrudRepository<BicDirectoryEntry, Long> {
}
