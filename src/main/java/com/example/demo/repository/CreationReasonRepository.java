package com.example.demo.repository;

import com.example.demo.models.CreationReason;
import org.springframework.data.repository.CrudRepository;

public interface CreationReasonRepository extends CrudRepository<CreationReason, Long> {
}
