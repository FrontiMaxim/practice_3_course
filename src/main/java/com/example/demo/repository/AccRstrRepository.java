package com.example.demo.repository;

import com.example.demo.models.AccRstr;
import org.springframework.data.repository.CrudRepository;

public interface AccRstrRepository extends CrudRepository<AccRstr, Long> {
}
